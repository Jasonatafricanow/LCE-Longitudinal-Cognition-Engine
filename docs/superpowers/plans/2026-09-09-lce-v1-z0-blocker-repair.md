# LCE V1 Z0 Blocker Repair R1

## Base and boundary

- Base: `16b014e1a1d3b4a225d4049d7df9615c93921dc4`
- Worktree: `C:\projects\w\lce-v1-close`
- Scope: B1-B8 plus N2/N1 documentation and Ruff cleanup.
- Preserve the frozen Raw Evidence → Semantic Block → vector → snapshot → bounded interpretation → worktree → Baseline → read chain.
- Do not add MR, Body, truth-judge, ontology, or recursive cognition behavior.

## Implementation sequence

1. Add adversarial tests for historical block state, effective structure identity, candidate-relevant support, interpreter authorization, invalidation, ordered failure, downstream recovery, and independent substrate execution.
2. Make Semantic Block state immutable by revision/state identity. Record snapshot-visible block state and vector digests in snapshots; make historical structure and candidate expansion resolve the recorded state. Keep current block lookup for the latest state and expose cutoff reconstruction through the substrate contract.
3. Canonicalize equivalent local observations for higher-order relations without removing observation lenses. Use stable effective support identities and make linked-structure diff a true prior/current delta.
4. Introduce focused Reference Memory contracts for evidence, semantic blocks/state, vectors, compiler progress, and invalidation. Inject the composed substrate into the compiler, discovery, adapter, invalidator, read API, and runtime. Keep SQLite as the default implementation and add an independent in-memory V1 backend test double.
5. Replace generated relation labels with a bounded interpreter port. Build the exact package from candidate snapshot structures, snapshot-bound block states, and authorized raw refs; reject unauthorized expansion and record truthful trace metadata. Keep promotion policy and Core authority unchanged.
6. Add candidate-relevant support fingerprints/deltas to worktrees. Record only novel qualifying point/structure/diff/temporal support, and never treat a new global snapshot alone as support.
7. Replace invalidation text-copy correction with localized rebuild and normal candidate interpretation/promotion. Suppress affected HEADs until valid support exists; do not alter unrelated lineages.
8. Add durable pending failure and stage progress. Make compiler unit commit idempotent, preserve the earliest failed input barrier, and make runtime resume downstream stages independently of compiler replay. Use persisted stage keys/fingerprints to avoid duplicate snapshots, supports, and Baseline revisions.
9. Document the deterministic semantic/vector fallback and clean only mechanical Ruff findings.
10. Run targeted tests after each bounded group, then the full required verification, fresh package export/install/import, restart/failure/alternate-substrate/invalidation checks, and write `LCE_V1_Z0_BLOCKER_REPAIR_REPORT.md` without claiming product closure.

## Commit groups

- R1 historical snapshot state
- R2/R3 structure and support identity
- R4/R5 bounded interpretation and invalidation
- R6/R7 recovery semantics
- R8 substrate contracts and injection
- R9-R11 diff semantics, fallback documentation, lint, and repair report
